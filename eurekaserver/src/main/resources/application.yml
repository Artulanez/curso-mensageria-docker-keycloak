spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: cursoms-eureka-user
      password: crs-uoe-makerussure

server:
  port: 8761

euraka:
  client:
    register-with-eureka: false
    fetch-registry: false
